# Enforce the engines.npm constraint (>=11). npm 10.x omits optional
# platform-specific packages (e.g. @rolldown/binding-*) from the lockfile
# (npm/cli#4828), producing an incomplete package-lock.json. Refusing old
# npm here stops a broken lockfile from ever being regenerated again.
engine-strict=true
