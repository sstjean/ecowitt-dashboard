# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Environment / secrets (FR-055 — no value-bearing config in source control)
.env
.env.local
.env.*.local

# Kiosk provisioning secret (operator-supplied WiFi PSK) — only .env.example is committed
deploy/kiosk/.env

# SQLite data + WAL sidecars
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal
data/

# Coverage
coverage/

# Playwright (UI e2e) output + local browser-tool scratch
test-results/
playwright-report/
blob-report/
.playwright/
.playwright-mcp/

# Logs / temp
*.log
*.tmp
*.swp

# OS / editor
.DS_Store
Thumbs.db

# Local native-run SQLite data
.local/

# Watchdog (009) local overrides — never commit
deploy/watchdog/.env

# Python coverage / cache artifacts (watchdog test gate)
.coverage
.coverage.*
__pycache__/
*.pyc
